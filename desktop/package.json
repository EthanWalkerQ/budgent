{
  "name": "budgent-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Budgent Desktop — a non-custodial agent wallet (Electron). Live on-chain state + real payments via budgent-sdk.",
  "main": "main.js",
  "scripts": {
    "start": "electron ."
  },
  "dependencies": {
    "budgent-sdk": "^0.1.0"
  },
  "devDependencies": {
    "electron": "^31.0.0"
  }
}

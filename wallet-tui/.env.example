# Copy to .env and fill in. .env is gitignored — never commit secrets.
BUDGENT_BASE_URL=https://budgent.money
BUDGENT_VAULT_ID=cmqif4af40000e9birfj0zp8h

# Needed ONLY to SEND real payments. Mint a scoped key first (see README "Arming"),
# then paste the keyId + hmacSecret it returns:
BUDGENT_KEY_ID=
BUDGENT_HMAC_SECRET=
